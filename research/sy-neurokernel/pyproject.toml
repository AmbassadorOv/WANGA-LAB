[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sy-neurokernel"
version = "0.2.0"
description = "Deterministic provenance-preserving 22-address relation kernel"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["sy_neurokernel*"]
