# TNRL kernel example
source m1 mishnah "source proposition"
source g2 gemara "reasoning context"

term x:Person
predicate Human(Person)

assert Human(x) [m1,g2]

operator q1 = kushya(fact:Human(x))
operator t1 = terutz(q1)
